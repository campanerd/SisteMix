info:
  name: Atualizar Status Parcela
  type: http
  seq: 20

http:
  method: PATCH
  url: http://localhost:8080/parcelas/{{IdParcela}}/status
  body:
    type: json
    data: |-
      {
        "status": "PAGO"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
