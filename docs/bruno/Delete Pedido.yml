info:
  name: Delete Pedido
  type: http
  seq: 15

http:
  method: DELETE
  url: http://localhost:8080/pedidos/{{IdPedido}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
