info:
  name: Update Cliente
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/clientes
  body:
    type: json
    data: |-
      {
        "id": 1,
        "nome": "Jorge Washington",
        "telefone": "11988877888",
        "email": "jorge.novo@email.com"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
