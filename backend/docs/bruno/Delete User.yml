info:
  name: Delete User
  type: http
  seq: 25

http:
  method: DELETE
  url: http://localhost:8080/api/users/{{IdUser}}
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: {{token}}

runtime:
  variables:
    - name: IdUser
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
