info:
  name: Create User
  type: http
  seq: 21

http:
  method: POST
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
        "name": "Davi Teste",
        "email": "davi.teste@sistemix.com",
        "password": "123456",
        "role": "ROLE_ADMIN"
      }
  auth:
    type: bearer
    token: {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
