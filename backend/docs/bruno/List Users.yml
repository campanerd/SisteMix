info:
  name: List Users
  type: http
  seq: 22

http:
  method: GET
  url: http://localhost:8080/api/users
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
