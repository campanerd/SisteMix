info:
  name: Login
  type: http
  seq: 22

http:
  method: POST
  url: http://localhost:8080/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "davi.teste@sistemix.com",
        "password": "SenhaNova"
      }
  auth:
    type: bearer
    token: {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
