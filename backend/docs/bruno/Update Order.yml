info:
  name: Update Order
  type: http
  seq: 14

http:
  method: PUT
  url: http://localhost:8080/api/orders
  body:
    type: json
    data: |-
      {
        "id": 1,
        "issueDate": null,
        "orderDate": null,
        "totalAmount": 2000.00,
        "notes": "Pedido atualizado",
        "sellerId": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
