info:
  name: List Installments
  type: http
  seq: 16

http:
  method: GET
  url: http://localhost:8080/api/installments
  auth:
    type: bearer
    token: {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
