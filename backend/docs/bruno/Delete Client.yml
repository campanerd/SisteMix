info:
  name: Delete Client
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/api/clients/{{IdClient}}
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
