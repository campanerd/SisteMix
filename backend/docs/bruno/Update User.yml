info:
  name: Update User
  type: http
  seq: 23

http:
  method: PUT
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
        "id": 3,
        "name": "Davi Testando",
        "email": "davi.teste@sistemix.com",
        "password": "SenhaNova",
        "role": "ROLE_ADMIN"
      }
  auth:
    type: bearer
    token: {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
