info:
  name: Get Order History
  type: http
  seq: 26

http:
  method: GET
  url: http://localhost:8080/api/orders/{{IdOrder}}/history
  auth:
    type: bearer
    token: {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
