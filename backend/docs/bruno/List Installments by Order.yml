info:
  name: List Installments by Order
  type: http
  seq: 18

http:
  method: GET
  url: http://localhost:8080/installments/order/{{IdOrder}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
