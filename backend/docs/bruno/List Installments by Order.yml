info:
  name: List Installments by Order
  type: http
  seq: 18

http:
  method: GET
  url: http://localhost:8080/api/installments/order/{{IdOrder}}
  auth:
    type: bearer
    token: {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
