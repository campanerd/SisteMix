info:
  name: Get Client
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/clients/{{IdCliente}}
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
