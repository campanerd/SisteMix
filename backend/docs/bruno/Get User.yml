info:
  name: Get User
  type: http
  seq: 24

http:
  method: GET
  url: http://localhost:8080/api/users/{{IdUser}}
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: {{token}}

runtime:
  variables:
    - name: IdUser
      value: "2"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
