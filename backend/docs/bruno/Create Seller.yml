info:
  name: Create Seller
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/api/sellers
  body:
    type: json
    data: |-
      {
        "name": "Ana Lima",
        "cpf": "661.222.333-44",
        "phone": "11977990001"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
