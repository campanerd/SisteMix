info:
  name: Create Seller
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/api/vendedores
  body:
    type: json
    data: |-
      {
        "nome": "Ana Lima",
        "cpf": "661.222.333-44",
        "telefone": "11977990001"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
